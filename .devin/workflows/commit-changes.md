---
description: Stage, commit, push changes, and create a PR to master
---

Use the @commit-changes skill to stage, commit, push changes, and create a PR to master. Provide the folder path as an argument (e.g., /commit-changes assets/styles), or run without arguments to commit all changes.
